module.exports = {
    init: init
};

function init(emitter) {
  var list_options = {
    name: 'pkchallenge',
    list_name: 'challenge',
    help: 'Usage: !' + name + " @user. challenge the mentioned user.",
    help_on_empty: true
  };
}
